version: '3.8'

services:
  # 比特云 NAS (Alist 独立存储服务)
  alist:
    image: xhofe/alist:latest
    container_name: bitpulse-nas
    restart: always
    ports:
      - "5244:5244"
    volumes:
      - ./data:/opt/alist/data
    environment:
      - PUID=0
      - PGID=0
      - UMASK=022